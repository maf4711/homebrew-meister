# meister / MeisterAI 6.32

- Share one Homebrew prefix with every admin user before `brew update`: group `admin`, setgid, an inherited write ACL, and `git safe.directory` for `/usr/bin/git` and Homebrew's git. The tree is not chowned.
- `homebrew-share` runs the same heal. A run without a sudo ticket skips it instead of waiting for a password.

## Recap
Both CLI variants share the Homebrew prefix for admin users. Update with `brew update && brew reinstall maf4711/meister/meister`.
