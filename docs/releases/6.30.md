# Meister v6.30

Time Machine **local snapshot** checks are gone. Cleanup, APFS thinning, and
the snapshot count in Time Machine health no longer run. Destination and
backup-age reporting stay.

Install or update:

```sh
brew reinstall maf4711/meister/meister
MeisterAI --version
```

Recap: no more TM local-snapshot enumeration or thinning.
