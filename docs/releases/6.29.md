# Meister v6.29

AI-Heal is **on**. Allowlisted commands run, then the module is retested.
Opt out with `AI_HEAL_EXECUTE=false` or `--ai-heal-suggest`.

Install or update:

```sh
brew reinstall maf4711/meister/meister
MeisterAI --version
```

Recap: AI-Heal executes by default; suggest-only is the opt-out.
