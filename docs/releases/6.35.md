# Meister 6.35

AI Updates now logs the failing updater, exit code and affected client so AI-Heal receives useful evidence. Raw updater output remains suppressed to protect account information.

Homebrew ownership checks use installed app artifact paths. ChatGPT packages using the `com.openai.codex` bundle identifier no longer produce an incorrect unmanaged-app warning. Renamed copies remain unmanaged.

Validation: regression tests cover retry exit codes, private-output suppression, AI-Heal evidence and app ownership.

## Recap

Clearer AI update failures and accurate Homebrew app ownership.
